# Risk Register

| ID | Description | Severity | Affected area | Evidence | Mitigation | Owner/next action | State |
|---|---|---|---|---|---|---|---|
| RISK-01 | `IMPLEMENTATION_PACK.md` §2.2 assumes a live Production breakdown substrate (`production.breakdowns`, RPCs, audit log) that does not exist in code yet — `Monarch-Production-Module` repo is placeholder-only. | MEDIUM | Cross-module integration (§2.2, §13) | Cloned sibling repo, found only `index.html`/README. | Build Maintenance fully standalone per §2.1; store only reference-ID columns; do not block on Production substrate. Re-check when Production module ships real schema. | Boss / re-check before enabling any live cross-reference | OPEN |
| RISK-02 | LOTO/PTW plant SOP (issuer/performer/permit authority/authorized-person matrix) is unresolved (PENDING-01). | HIGH (safety-adjacent) | Safety gate (§14) | Pack explicitly marks PENDING-01. | Build feature-flagged seam (PTW Required Y/N, proof reference) without inventing authority. Governed work path blocked until Boss supplies SOP. | Boss to supply plant SOP evidence | OPEN |
| RISK-03 | Recurrence threshold/window values unresolved (PENDING-04). | LOW | Recurrence detection (§18) | Pack explicitly marks PENDING-04. | Ship configurable hybrid model with no default hard-coded threshold treated as business truth; require explicit config before flag is trusted operationally. | Boss to approve threshold/window | OPEN |
| RISK-04 | Exact granular permission matrix (action/field level) beyond the 2 locked roles is unresolved (PENDING-03). | MEDIUM | RBAC (§29) | Pack explicitly marks PENDING-03. Loop 32: read every RLS SELECT policy in the schema (`pg_policies`) specifically for read-side exposure (this batch's prior loops audited write/authority paths only). 4 tables use `using (true)` — open to ANY authenticated user, not staff-scoped: `cases`, `evidence`, `safety_stops`, `production_boundary_events`. `cases_select`'s own migration comment says "All staff can see all open work (§22 dashboard requirement)" but the actual policy is broader than "staff" — any signed-in user, including a non-staff reporter, can read every case in the system, not just their own. `evidence` was already reviewed and deliberately accepted in Loop 18 (documented in `evidence-panel.tsx`). `safety_stops`/`production_boundary_events` carry no comment justifying `true` either way, but plausibly make operational sense open (plant-wide visibility of an active safety stop is arguably a safety benefit, not a leak) — every other actor-scoped table in the schema instead follows an `is_staff() OR <owner column> = auth.uid()` pattern, which is the asymmetry worth the Boss's attention. | Implement the 2 locked roles + server-side checks for every locked authority boundary explicitly stated in the pack (e.g. ₹12,000 split, reopen = Exec+Manager); do not invent finer-grained rules beyond what's locked. Read-side scoping for `cases`/`safety_stops`/`production_boundary_events` is deliberately NOT narrowed here — doing so would mean guessing at PENDING-03's matrix rather than receiving it, and could itself break a legitimate need (e.g. a reporter tracking their own case, or plant-wide safety-stop visibility) without evidence either way. | Boss to verify before production rollout — specifically, confirm whether non-staff read access to all cases/safety-stops/production-boundary-events (vs. staff-only, or reporter-scoped) is the intended design | OPEN |
| RISK-05 | No browser E2E had ever run against the app — this sandbox's egress proxy policy-blocks `*.supabase.co`, so nothing here could drive a real sign-in. The UI layer was therefore never verified end-to-end; RISK-10 (broken login) and RISK-12 (a dead-end button) both hid in exactly that gap. | MEDIUM | Test evidence (§37, §38, §39) | Direct curl/Playwright attempts got `CONNECT tunnel failed, response 403` from the sandbox's own proxy; confirmed via `/__agentproxy/status` (policy denial, not an app bug). | Loop 11: replaced the never-run `e2e/smoke.mjs` script with a real `@playwright/test` suite (8 tests, `e2e/*.spec.ts`) and wired it into CI as its own `e2e` job — GitHub Actions has normal egress, so the browser genuinely signs in there. Locally the 2 signed-out specs now actually run and pass (first browser tests ever to pass in this repo); the 6 sign-in specs fail here only at the Supabase network call, with the login page's own error surfaced (`Failed to fetch (…supabase.co)`) rather than a bare timeout. | RESOLVED — the CI `e2e` job is green (8/8) on PR #8. The first run was 6/8; both failures were wrong assertions in the new test code, not app defects, and the four sign-in flows passed first time. The UI layer is now covered on every push. |
| RISK-06 | Supabase anon key and Sentry DSN are committed as source fallbacks (`src/lib/supabase/config.ts`, `src/instrumentation*.ts`) instead of Vercel dashboard env vars. | LOW | Config hygiene (§19.6) | The Vercel MCP tool set available in this session has no operation to set project environment variables (only project creation/linking and file-deploy). Both values are non-secret by their platform's own model (anon key protected by RLS; DSN is submit-only) — not the same category as a service-role key or auth token, neither of which was committed. | Optional cleanup: an operator with Vercel dashboard access can move these into real env vars and delete the fallback constants; not functionally blocking. | OPEN |
| RISK-07 | No automated test framework (unit/integration/E2E runner) is wired into the repo yet — `npm test` does not exist. §37's full test matrix (concurrent-accept race, QC rejection loop, waiting escalation timers, spare traceability, PM overdue, etc.) is unimplemented as automated tests; only a subset was manually verified once (see CHANGELOG Loop 1). | MEDIUM | Test coverage (§37, §39) | STATUS.md "Test status" section; only 6 scenarios manually verified via `execute_sql`, out of the full matrix in §37. | Loop 6: added a Vitest integration suite (17 tests, `tests/*.test.ts`) covering Scenario A/B/C, negative-path transitions, idempotency, append-only enforcement, RLS bypass denials, ownership race, and WAITING — wired into CI (`npm test` step in `.github/workflows/ci.yml`). Structurally verified in this sandbox (loads, type-checks, all 17 execute and fail identically only on the network call — see tests/README.md); real pass/fail signal is from the CI run, not this sandbox. Still missing: PM, spares, notifications/escalation, duplicate/false-complaint tests (don't exist as features yet either). | PARTIALLY RESOLVED |

| RISK-08 | Vercel project had SSO (Vercel Authentication) protection on for all deployments except custom domains — plant users with no Vercel account could not have opened the app at all. | HIGH (would have blocked all real users) | Deployment access (§30 mobile-first execution) | `get_project_deployment_protection` showed `ssoProtection.enabled: true, deploymentType: "all_except_custom_domains"`; confirmed by a `/cases` fetch redirecting to `vercel.com/sso-api` instead of the app's own `/login`. | Disabled SSO protection at the project level (Loop 2); app-level Supabase auth remains the real access boundary. | Resolved Loop 2 | RESOLVED |
| RISK-09 | `src/proxy.ts` HTML-redirected unauthenticated `/api/*` requests to `/login` instead of letting the route execute. | MEDIUM (would have broken every future API route silently) | Auth middleware (§29) | Confirmed via the new `/api/sentry-test` route returning a redirect instead of JSON before the fix. | Excluded `/api/*` from the page-auth redirect in `proxy.ts` (Loop 2). | Resolved Loop 2 | RESOLVED |

| RISK-10 | Login broken in production: Supabase Auth (GoTrue) returned "Database error querying schema" for every test account. | HIGH (blocked all login) | Auth (§29) | Confirmed via Supabase `auth_logs`: `error finding user: sql: Scan error on column index 8, name "email_change": converting NULL to string is unsupported`. Root cause: the 3 test accounts were seeded via raw SQL (not the real signup/Admin API path), leaving several GoTrue-internal token columns (`email_change`, `email_change_token_new`, `email_change_token_current`, `phone_change`, `phone_change_token`, `reauthentication_token`) NULL instead of GoTrue's expected `''`. | Backfilled `''` into every NULL of those columns for the existing accounts. Verified fixed via a temporary server-side diagnostic route calling the real `signInWithPassword` flow (all 3 accounts now return a session) — see CHANGELOG. Route removed after verification. | RESOLVED |
| RISK-11 | `transition_case`'s QC guard (added in Loop 5) used `coalesce(v_qc_required, false)`, so a case whose `qc_required` was never explicitly decided (NULL) could bypass the QC gate via a direct `transition_case` call to `MAINTENANCE_RELEASED`, even though the UI only offers that path when `qc_required` is explicitly `false`. | MEDIUM | QC gate (§12, §13) | Found by re-auditing the Loop 5 guard logic against the UI's own gating. | Guard now requires `v_qc_required is distinct from false` (blocks both `true` and `NULL`, only allows an explicit `false`). Verified via `execute_sql`: undecided `qc_required` correctly blocks direct release; setting it to `false` then allows it. | RESOLVED |
| RISK-12 | The case-detail UI showed a "Record restoration" button while a case was `TEMPORARILY_RESTORED`, but the locked lifecycle graph has no `TEMPORARILY_RESTORED → TECHNICALLY_RESTORED` edge (only `IN_REPAIR` does) — clicking it always failed with `INVALID_TRANSITION`. | HIGH (dead-end button on a core flow) | Lifecycle UI (§10, Scenario C) | Found by cross-checking every form's status-gating condition against `status_transitions`. | Replaced with a `FollowUpButton` matching Scenario C ("follow-up" = resume to `IN_REPAIR`/`DIAGNOSING`) when `TEMPORARILY_RESTORED`; the restoration form now only shows in `IN_REPAIR`. Verified full Scenario C end-to-end via `execute_sql`. | RESOLVED |
| RISK-13 | `maintenance.is_manager()` (0002) returned `NULL` — not `false` — for any authenticated caller with no `maintenance.staff` row, because `current_staff_role() = 'MAINTENANCE_MANAGER'` propagates NULL. Its only prior use was inside an RLS `with check`, where NULL is already treated as reject, so it was never exploitable until Loop 8's `approve_spare_request` used the standard `if not maintenance.is_manager() then raise exception` guard pattern — PL/pgSQL's `IF NULL` silently skips the branch, so a non-staff authenticated caller could have fallen through the Manager-only authorization check on a ₹12,000+ spare approval undetected. | HIGH (financial-authority bypass) | §3.3 financial/spare authority, `approve_spare_request` (§16, Loop 8) | Reproduced live via `execute_sql`: before the fix, a non-staff caller's `approve_spare_request` call proceeded past the guard instead of raising `FORBIDDEN`. Same class of defect as RISK-11 (guard against NULL, not just explicit false). | Fixed at the source (`0010_maintenance_is_manager_null_fix.sql`): `is_manager()` now returns `coalesce(current_staff_role() = 'MAINTENANCE_MANAGER', false)`, so every future `if not is_manager()` call site anywhere in this schema is safe by construction. Re-verified live: non-staff and staff-Executive callers now both correctly rejected; only Manager proceeds. | RESOLVED |
| RISK-14 | Loop 9's `transition_case` rewrite (0011, adding the DUPLICATE guard) was based on a stale copy of the function predating both the §13 QC gate (Loop 5, 0007) and its RISK-11 NULL-safety fix — that fix was applied live but never captured in a numbered migration file in this repo, making it easy to miss when re-copying the function body. The rewrite silently dropped the QC gate entirely: a direct `TECHNICALLY_RESTORED -> MAINTENANCE_RELEASED` transition would have succeeded even with `qc_required = true`. | HIGH (safety/QC gate bypass) | §13 QC gate, `transition_case` | Caught by CI on PR #5: `qc-and-restoration.test.ts`'s two QC-gate tests failed (`expected null not to be null` where a `QC_GATE` error was expected). Not a sandbox/test artifact — reproduced live via `execute_sql` before fixing (a `qc_required=true` case transitioned to `MAINTENANCE_RELEASED` with no error). | Restored the QC gate (RISK-11 NULL-safe form: `v_qc_required is distinct from false`) together with the DUPLICATE guard in one function (`0012_maintenance_qc_gate_regression_fix.sql`). Re-verified live: `qc_required = true`, `qc_required = null`, and `qc_required = false` all behave correctly again, and the DUPLICATE guard still fires. | RESOLVED |

| RISK-15 | `maintenance.case_current_impact` (0016) is the first VIEW in this schema and shipped with Postgres's default behaviour: a view executes with its OWNER's privileges, and the owner (`postgres`) is not subject to RLS. The view therefore read straight past the `case_impact_records` SELECT policy (`is_staff()`), exposing every case's production-impact data to any authenticated user, including the non-staff technician identity. | HIGH (RLS bypass / data exposure) | §25 KPI reporting, `case_current_impact` | Proven live, not inferred: with 2 impact rows present, a technician JWT reading an identical probe view created without `security_invoker` returned **2 rows**, while the same JWT reading the base table returned **0**. The probe view was dropped immediately after the measurement. | `0017_maintenance_impact_view_security_invoker.sql` sets `security_invoker = true`. Re-verified live: the technician JWT now returns 0 rows through the view while staff still sees the record. Permanent regression test added (`tests/kpi.test.ts`, "does not let a non-staff user read impact data through the view"). Standing precedent recorded for this repo: **every reporting view over an RLS-protected maintenance table must set `security_invoker`**, or the policy underneath it is decorative. | RESOLVED |
| RISK-16 | The Vitest suite signed in fresh on every `signInAs()` call. At 78 call sites it fired ~78 requests at Supabase GoTrue's `/token` endpoint inside ~80 seconds from a single CI IP, exceeding the project's auth rate limit — CI failed with 8 tests erroring `Request rate limit reached` at `signInWithPassword`. Compounded by `push: branches: ["**"]` + `pull_request` running two identical CI workflows per PR commit, doubling the load and running two suites concurrently against the same live Supabase project. Severity is real rather than cosmetic: the failure rate grew with every test added, and it produced red CI that looked like a product defect. | MEDIUM (false-red CI, shared-data races) | Test infrastructure (§37, §39), `.github/workflows/ci.yml` | CI job 101503661687 on PR #9: 45 passed / 8 failed, every failure identical at `tests/helpers.ts:23`. Not a flake — re-running would only have moved the failure to whichever file consumed the remaining quota. | One signed-in client cached per role (3 sign-ins per run instead of 78); the promise rather than the result is cached so concurrent first calls cannot race into two sign-ins, and a failed sign-in is evicted so one transient error cannot poison later tests. CI now runs on `push` to `main` only plus `pull_request`, with a `concurrency` group cancelling superseded runs so two suites can never race on the shared project. Verified: the next CI run was a single workflow, 53/53 green, plus a green `e2e` job. | RESOLVED |

| RISK-17 | `clearances_insert` (0002) allowed a direct client insert with `with check (is_staff() AND sent_to_qc_by = auth.uid())` — no check that the target case's status was `TECHNICALLY_RESTORED`. Any staff member could insert a `clearances` row for any case in any status, completely bypassing `send_to_qc`'s own status guard (`if v_status <> 'TECHNICALLY_RESTORED' then raise INVALID_OPERATION`). | MEDIUM (server-side enforcement gap on a locked QC boundary; not live-exploitable as a lifecycle bypass — see mitigation) | §12 QC gate, `clearances` | Found during Loop 25's systematic RLS-coverage sweep, live-verified before writing a test (never trust an RLS assumption without checking it live — the standing discipline since RISK-13/15): a direct insert against a case still sitting at `REPORTED` (never even acknowledged) succeeded, creating a `decision = 'PENDING'` row with no case-status check at all; the case's own status stayed `REPORTED` afterward, confirming the orphaned row could not move the case forward on its own — `qc_decision(CLEARED)` still calls `transition_case(..., 'MAINTENANCE_RELEASED')`, and that RPC's own `status_transitions` graph rejects the edge from anything but `TECHNICALLY_RESTORED`/`CLEARANCE_PENDING`. Same shape of bug as the one Loop 8 already fixed once for `spare_requests`/`spare_usage` (a direct-insert policy letting a client set fields a dedicated RPC was supposed to gate). | `0024_maintenance_clearances_rpc_only.sql`: `clearances_insert` is now `with check (false)` — RPC-only, matching every other financially/audit-sensitive table in this schema. Re-verified live: direct insert now fails with a `42501` RLS violation; `send_to_qc` (SECURITY DEFINER, bypasses RLS) still works end-to-end. Permanent regression tests added (`tests/observations-clearances-audit.test.ts`). | RESOLVED |

| RISK-18 | `case_assignments_insert` (0004) allowed a direct client self-insert with `with check (emergency_direct_start AND technician_user_id = auth.uid())` — `emergency_direct_start` is a plain client-supplied boolean on the INSERT itself, never checked against the target case's actual `emergency_confirmed` status. Any non-staff technician identity could self-insert an `is_active = true` `case_assignments` row on **any case in the system**, and via `page.tsx`'s `isAssignedTechnician` check, self-grant `canRecordIntervention`/`canRecordSpareUsage` — completely bypassing both §5.5's staff-mediated assignment and §6's two-step emergency confirmation gate. | HIGH (live-exploitable authority bypass, not merely a server-side enforcement gap) | §5.5 assignment authority, §6 emergency two-step, `case_assignments` | Found during Loop 26's systematic RLS-policy audit (every policy's live `qual`/`with_check` read against its own migration's comment). Live-verified as genuinely exploitable, not theoretical: signed in as the seeded non-staff technician identity, a direct insert with `emergency_direct_start = true` succeeded against a case whose `emergency_confirmed` was `false` (never even claimed), producing an `is_active = true` assignment row — the exact row `isAssignedTechnician` checks for. | `0025_maintenance_case_assignments_emergency_gate.sql`: the policy now also requires `exists (select 1 from cases where id = case_id and emergency_confirmed = true)`. Re-verified live in all three directions: the same insert on a non-emergency case now fails (`42501`); the identical insert succeeds once the case has genuinely been through `claim_emergency`/`confirm_emergency`; staff-mediated `assign_technician` (SECURITY DEFINER, bypasses RLS) is unaffected. Permanent regression tests added (`tests/emergency-and-notifications.test.ts`). | RESOLVED |
| RISK-19 | `cases_insert` (0002) validated only `with check (reporter_user_id = auth.uid())` on the single most consequential table in the schema. Every other column on `maintenance.cases` — `status`, both `emergency_claimed`/`emergency_confirmed` and their `_by`/`_at`/`_reason` columns, `qc_required`, `ptw_required`, `current_owner_user_id`, `acknowledged_by_user_id`, `priority`, `closed_at`, `closure_reason`, `duplicate_of_case_id`, and the production handoff flags — was fully client-writable at INSERT time. A `default` (e.g. `status default 'REPORTED'`) is not a constraint: it is silently overridden the moment a client supplies its own value for that column. | CRITICAL (strictly worse than RISK-18: no staff access, no RPC, and no prior case state required at all — a brand-new INSERT can fabricate a fully-closed case out of nothing, with none of the §4 LOCKED lifecycle graph's edges ever consulted, and none of the §6/§27 audit trail RPCs ever invoked) | §4 LOCKED lifecycle graph, §6 emergency two-step, §27 audit/append-only, `cases_insert` | Found during Loop 27's continuation of the RLS-policy audit (same method as RISK-18, applied to `cases_insert` specifically, per `APPROVAL_REPORT_LOOP_21_25.md` §H.5). Live-verified as the seeded non-staff technician identity: (1) a direct insert with `emergency_confirmed = true` succeeded, self-confirming a fake emergency with zero claim/confirm ceremony; (2) a direct insert with `status = 'CLOSED'`, a fabricated `closure_reason`, and `closed_at` succeeded, producing case `MC-003975` — a fully-formed "closed" case that never passed through a single `status_transitions` edge or a single `transition_case`/`acknowledge_case`/`record_restoration` RPC call. This also meant the just-shipped RISK-18 fix (0025) was independently circumventable: fake `emergency_confirmed = true` here first, then walk the (now "legitimate"-looking) `emergency_direct_start` path. | `0026_maintenance_cases_insert_column_lockdown.sql`: `cases_insert` rewritten as an allow-list — `reporter_user_id = auth.uid()` plus an explicit `is null`/`= false`/`= 'REPORTED'` check on every other column, matching exactly the fields the real intake form (`src/app/(app)/cases/new/page.tsx`) submits (`case_type`, `symptom`, `area`, `line`, `asset_known`, `major_complex_flag`, `reporter_user_id`). Re-verified live: both exploit inserts now fail with `42501`; the legitimate intake payload still succeeds and lands at `status = 'REPORTED'`, `emergency_confirmed = false`, `qc_required = null`, `current_owner_user_id = null`. All state-changing RPCs (`acknowledge_case`, `claim_emergency`, `confirm_emergency`, `transition_case`, `change_priority`) confirmed `SECURITY DEFINER` — they bypass RLS entirely and are unaffected by this tightened INSERT policy. 4 permanent regression tests added (`tests/lifecycle.test.ts`). | RESOLVED |
| RISK-20 | `case_assignments_insert` (fixed for the emergency gate in `0025`/RISK-18) still left `assigned_by_user_id`, `is_active`, and `deactivated_at` fully client-writable on the direct self-insert path. A self-service `emergency_direct_start` technician could set `assigned_by_user_id` to any real staff member's id, falsely attributing the assignment to staff mediation that never happened — the same "with_check validates one column, ignores the rest of the row" shape as RISK-18/RISK-19, applied to attribution rather than authority. | MEDIUM (an audit-trail integrity gap on a table §29 relies on — "the UI is not a security boundary" — not itself a lifecycle/authority bypass like RISK-18/RISK-19, but a forged assignment record that misrepresents whether staff mediated an emergency response) | §5.5 assignment authority/attribution, §29 audit trail, `case_assignments` | Found during Loop 28's continuation of the RLS-policy audit, this time reading `case_assignments_insert`'s full column list (not just the columns the 0025 fix already covered) against the table's own schema. Live-verified as the seeded non-staff technician identity on a genuinely confirmed emergency: a direct insert with `assigned_by_user_id` set to the real manager's staff id succeeded, producing a row that looks staff-mediated but wasn't. | `0027_maintenance_case_assignments_attribution_lockdown.sql`: `with_check` now also requires `assigned_by_user_id is null`, `is_active = true`, and `deactivated_at is null` — the only honest state a fresh self-service row can start in. Re-verified live: the forgery attempt now fails (`42501`); the legitimate self-insert (no attribution fields set) still succeeds. `assign_technician` (the staff-mediated path, `SECURITY DEFINER`) confirmed unaffected — it bypasses RLS and is the only place `assigned_by_user_id` is legitimately set. 1 new regression test added (`tests/emergency-and-notifications.test.ts`). | RESOLVED |
| RISK-21 | `record_spare_usage`'s `>₹12,000` Manager-approval gate (§3.3) only ran `if p_spare_request_id is not null` — but that parameter defaults to `NULL` and nothing forced a caller to supply it. Any staff member or actively assigned technician could record spare usage with zero request, zero approval, and zero identification of which spare was even used (`spare_usage` has no `spare_name` column of its own — the only place that's recorded is `spare_requests.spare_name`, via the link). Reachable not just via a direct RPC call but via this app's own shipped UI — `spares-panel.tsx`'s usage form defaulted to an explicit "(not linked to a request)" option. | HIGH (a UI-reachable, no-effort bypass of a LOCKED financial-authority boundary named explicitly in `CLAUDE.md`; also an unconditional violation of §16.1's "mandatory V1" spare-usage traceability chain, which starts with "which spare was used" — not a PENDING/interpretation matter) | §3.3 financial authority (₹12,000), §16.1 mandatory traceability, `spare_usage`/`record_spare_usage` | Found during Loop 29's continuation of the audit, this time reading every `SECURITY DEFINER` RPC's internal guards against its own documented intent rather than RLS policies. The 0009 migration's own comment described the gate as conditional ("if linked to a request...") without flagging that linkage was entirely optional. Live-verified as the seeded non-staff technician identity (assigned to a case): `record_spare_usage` with `p_spare_request_id` omitted succeeded outright, and separately confirmed the exact same call path is what the shipped UI's default dropdown selection produces. | `0028_maintenance_spare_usage_requires_request.sql`: `record_spare_usage` now raises `SPARE_REQUEST_REQUIRED` when `p_spare_request_id is null`, closing both the traceability gap (every usage now names its spare via the linked request) and the financial-authority bypass (the `>₹12,000` gate can no longer be sidestepped) with one check — not an invented new business rule, but enforcing the pack's own already-locked §16.1 mandate. Re-verified live: the omitted-link call now fails (`SPARE_REQUEST_REQUIRED`); a legitimate linked low-value usage still succeeds; a linked high-value unapproved request still correctly blocks with `APPROVAL_REQUIRED`. `spares-panel.tsx` updated to remove the "(not linked to a request)" option and guide the user to raise a request first when none exist. 1 new regression test (`tests/spares.test.ts`); 2 existing tests updated to link a request where they previously (incorrectly) exercised the unlinked path as if it were legitimate. | RESOLVED |
| RISK-22 | `record_intervention`'s actor-eligibility check (Loop 3, migration 0004) was `is_staff() OR p_technician_user_id = v_actor` — it never verified the caller actually held an active `case_assignments` row for the case, unlike `record_spare_usage`'s established pattern (Loop 8, migration 0009), whose own comment describes `record_intervention`'s intent as "staff, or the actively assigned technician on this case" — a documented intent the original code never implemented. Compounded by a NULL-propagation bug: `p_technician_user_id` defaults to `NULL`, and `NULL = v_actor` evaluates to `NULL` (not `false`) in SQL, so `not (false or NULL)` is `NULL`, and PL/pgSQL's `if NULL then` does not raise — simply omitting the parameter also silently passed the check. | HIGH (live-exploitable audit-trail forgery: any signed-in non-staff user, with zero assignment to a case, could fabricate an intervention record on ANY case in the system — this app's own UI already gated the form correctly behind `isAssignedTechnician`/`isStaffRow`, so this was a pure server-side enforcement gap, exactly what §29 warns against) | §5.5 assignment authority, §9 intervention data model, §29 audit trail, `interventions`/`record_intervention` | Found during Loop 30's continuation of the `SECURITY DEFINER` RPC audit, comparing `record_intervention`'s actual check against migration 0009's own description of its intent. Live-verified as the seeded non-staff technician identity with zero assignment to a fresh case: `record_intervention` succeeded both with `p_technician_user_id` explicitly set to their own id and with it omitted entirely (the NULL-propagation variant). | `0029_maintenance_record_intervention_assignment_check.sql`: the actor check now requires an active `case_assignments` row (matching `record_spare_usage`'s pattern) instead of a client-suppliable id comparison; a separate explicit check still blocks a non-staff caller from claiming to be a *different* technician than themselves. Re-verified live in all four directions: unassigned self-attribution now fails; unassigned omitted-id now fails; a genuinely assigned technician's self-recording still succeeds; a genuinely assigned technician impersonating a different technician still fails; staff-mediated recording on behalf of anyone (no assignment required) still succeeds. 2 new regression tests added (`tests/assignment-and-waiting.test.ts`); no UI change needed (already correct). | RESOLVED |

| RISK-23 | `qc_decision` (Loop 5, migration 0007) guarded only on `is_staff()`, so any `MAINTENANCE_EXECUTIVE` or `MAINTENANCE_MANAGER` could set a clearance to `CLEARED`/`REJECTED` and drive the case to `MAINTENANCE_RELEASED`. The complete self-clearing loop `Maintenance → send_to_qc → Maintenance → CLEARED` was reachable in the running system. The locked pack forbids this in three separate places — §1 ("Maintenance does NOT own ... QC product disposition / clearance truth"), §43 item 8 ("QC truth remains QC-owned"), §19.15 (Claude Code must not "grant QC clearance") — so this was never an interpretation question. | CRITICAL (a LOCKED cross-module authority boundary held entirely by the module it was meant to constrain; every QC clearance ever recorded in this system was made by a Maintenance identity) | §1 / §12 / §43.8 QC ownership, `qc_decision` | Found by the Boss-supplied forensic remediation brief (F-01) and confirmed against HEAD before any change: `qc_decision` is defined once in 0007 and never redefined by 0012/0019/0024. Two adjacent controls were checked and found **correct**, so the fix had to be surgical: `send_to_qc` properly requires staff + `TECHNICALLY_RESTORED` (contract-correct per §12), and `transition_case`'s direct `TECHNICALLY_RESTORED → MAINTENANCE_RELEASED` edge is properly gated on `qc_required` being explicitly `false`. The defect was precisely and only the actor check inside `qc_decision`. | `0031_maintenance_qc_authority_boundary.sql`. The pack defines the prohibition but deliberately leaves the positive authority open (§12 "Exact plant QC permit/authority remains evidence-controlled"; §3.1 has only two software roles; §33 forbids depending on a live QC API), and `DB VERIFIED` confirmed no QC role/table/identity existed anywhere to reuse. So the fix is the minimum safe authorization seam the brief itself authorises: `maintenance.qc_authority`, an explicit **empty-by-default** allowlist granted out-of-band by the service role exactly as `maintenance.staff` is — a list of user ids, inventing no plant SOP, no permit mechanics and no role semantics. `qc_decision` now refuses any `is_staff()` identity **first and unconditionally**, then requires an active grant, so even adding a Maintenance member to the allowlist cannot recreate the loop. It also now writes an `audit_log` row; the 0007 version wrote none, so the QC actor was absent from the audit trail entirely. Verified live against four real identities: Executive → `FORBIDDEN … does not own QC clearance`; Manager → same; non-staff without grant → `FORBIDDEN … granted QC authority`; granted QC identity → passed both gates to `CLEARANCE_NOT_FOUND`. 6 regression tests added (`tests/forensic-authorization.test.ts`). **Deliberate open consequence:** the allowlist is empty, so QC-required cases now stop at `CLEARANCE_PENDING` until the Boss names the real QC identities — fail-closed by design, and tracked as the first blocker in `FORENSIC_REMEDIATION_FINAL.md` §16. | RESOLVED (boundary enforced; grant list awaiting Boss evidence) |
| RISK-24 | Four SELECT policies were `USING (true)` — `cases`, `evidence`, `safety_stops`, `production_boundary_events` — so **any** authenticated account, including one with no `maintenance.staff` row at all, could read the entire plant's maintenance history. Concretely: `/cases` lists the 50 newest cases with no owner/reporter filter, so an unrelated signed-in user saw every case number, symptom, area, line, priority and owner, plus every safety-stop reason and every production-boundary record. Frontend gating did not restrict any of it. | HIGH (broad unauthorized read exposure of operational safety and production context across the whole module; the exact "frontend hiding is not authorization" failure §29 warns about) | §29 authorization, §22 dashboard visibility, `cases`/`evidence`/`safety_stops`/`production_boundary_events` | First surfaced in Loop 32 as a read-exposure asymmetry and **deliberately not fixed then** — it was logged against RISK-04 pending PENDING-03's granular permission matrix, on the reasoning that narrowing reads would mean guessing at that matrix. The forensic brief (F-02/03/04) reframed it correctly: least privilege does not require the full matrix, only the scopes the pack already backs. Live-verified before the change from `pg_policies`; every other table in the schema was already properly scoped. | `0032_maintenance_read_scope_least_privilege.sql`. One derived predicate, not a guess: Maintenance staff see all cases (§22's shift dashboard needs plant-wide visibility, and 0002's own comment already states the intent as "All staff can see all open work"); the reporter sees their own case (§5/§7 — they report it and are notified about it); an assigned technician sees the case they are assigned to (§3.1 — technicians hold authenticated execution identities and record work against the case). This is the shape the schema already used elsewhere (`case_assignments_select`/`interventions_select` are both `is_staff() OR <actor> = auth.uid()`), so it introduces no new authorization concept. Evidence, safety stops and production-boundary events inherit their case's scope via `maintenance.can_read_case()` (SECURITY DEFINER, so it neither recurses into the `cases` policy nor is blocked by the child tables' own RLS). **Write policies deliberately untouched** — `safety_stops` and `production_boundary_events` keep `with_check (false)`, still RPC-only. Verified live by counting rows visible per identity: an unrelated authenticated identity went from 7,592 cases / 211 evidence / 355 safety stops / 138 boundary events to **0 / 0 / 0 / 0**, while staff still see everything and an assigned technician still sees their own work. 7 regression tests added covering staff, reporter, assignee, unrelated identity, and write-protection preservation. | RESOLVED |

| RISK-25 | An **INTERNAL** wait can never reach the 24h escalation clock at all — not "rarely", but structurally. `mark_wait_resolved` is the only function in the schema that sets `waits.resume_ready_at`, and it explicitly refuses any wait whose `reason_type <> 'EXTERNAL'` (`INVALID_OPERATION`). `run_escalation_scan`'s 24h branch selects only `where resume_ready_at is not null`. So an INTERNAL Maintenance dependency can sit open indefinitely and no `WAIT_ESCALATION_24H` or `WAIT_MANAGER_REMINDER_24H` will ever be produced for it, for anyone. | **OPEN — evidence-controlled, deliberately NOT fixed.** Operationally significant (a third of all waits in this project are INTERNAL and none can ever escalate), but fixing it requires deciding *when* an INTERNAL wait's 24h clock starts — a threshold application the pack does not state. Inventing one is exactly what `IMPLEMENTATION_PACK.md` §19.15 forbids ("invent SLA/threshold values not approved in this pack"). | §7.2 waiting/resume rules, §23/§24 notification lists, `waits`/`run_escalation_scan` | Found in Loop 37 by auditing the escalation paths as a set — no prior loop had. Live-verified: of 320 waits, **EXTERNAL 213/213 have `resume_ready_at`; INTERNAL 0/107 do**. Not an artifact of test data — the code path is unreachable by construction, confirmed by reading `mark_wait_resolved`'s explicit `reason_type <> 'EXTERNAL'` guard. The surrounding machinery was verified healthy in the same loop, so this is a specific gap and not general rot: all 12 notification types have fired, and all **393 pg_cron runs across the three scheduled scans succeeded with zero failures**. | **The pack genuinely conflicts with itself here, which is why this is a question and not a fix.** §7.2 (the specific WAITING rule) scopes the escalation to "Resume-ready with no required action for 24h" — under that reading the current behaviour is exactly correct, since an INTERNAL wait has no route to resume-ready. But §23's locked-notification list and §24's AUTO list both say "24h normal escalation" with **no** such scoping. §32 item 15 groups them as "Resume-ready + escalation + reminders", leaning toward §7.2. The implementation follows the most specific section, which is defensible; the operational consequence is that internally-blocked work is invisible to escalation forever, which is unlikely to be what a plant wants. Boss evidence needed: does an INTERNAL wait escalate, and if so from what instant — `entered_at`, or something else? Until then, `tests/escalation-coverage.test.ts` pins the current behaviour explicitly so it stays a documented decision rather than drifting into an accident, and so nobody closes it by invention. | OPEN (PENDING Boss evidence) |

| RISK-26 | Three notification sites delivered the **same notification twice to the same person for one event**: they sent to the case owner (or PM-responsible person) and then looped every active Maintenance Manager. When the owner IS a Manager — entirely normal, a Manager can own a case — that person appeared in both sets and received two byte-identical rows. Sites: `run_escalation_scan`'s 24h wait escalation, its 1h emergency escalation, and `run_pm_scan`'s PM-overdue alert. | MEDIUM (a direct violation of two explicit §23 sentences — "Notifications are event-driven, not spam-driven" and "Notification delivery must be idempotent and auditable" — not a security or authority defect, but the kind of alert-fatigue bug that makes a real escalation get ignored) | §23 notifications/escalation, §28 (escalation notification creation), `run_escalation_scan`, `run_pm_scan` | Found in Loop 38 by asking the live data whether §23's idempotency claim actually held, rather than reading the code: grouped `notifications` by (type, recipient, case) looking for counts > 1. Two duplicate pairs surfaced on case MC-000077. **The evidence rules out a concurrency explanation**: both pairs carry timestamps identical to the MICROSECOND (`09:54:18.629383` twice, `09:54:55.425613` twice), which means one transaction and one scan pass — not two overlapping cron runs. The case's own `CASE_ACKNOWLEDGED` row ("acknowledged by Loop1 Test Manager") confirms a Manager owned it at that moment. Scale matters: with N active Managers an owner-Manager receives 2 while every other Manager receives 1, on every escalation, indefinitely. | `0035_maintenance_notification_recipient_dedupe.sql`: a single `case_notification_recipients(p_extra uuid)` helper returns the DISTINCT union of the extra recipient and every active Manager, and all three sites now iterate that set instead of sending twice. One place now answers "who hears about this case event", so a future recipient can be added without re-creating the collision. Escalation rules, thresholds, guards and once-only semantics (`last_escalated_at`/`emergency_escalated_at`) are byte-for-byte unchanged — only the recipient collection changed. Verified live across four shapes: owner-is-the-Manager → **1 recipient (was 2)**; owner is a distinct Executive → 2, both present; no owner (Manager-reminder path) → 1; non-staff owner → 2. The fix removes the duplicate **without dropping anyone**, which was the risk in fixing it. Three other Manager-notifying sites were checked and deliberately left alone — the handover `CASE_UNASSIGNED` path (0014), the production-boundary breach (0015) and the safety-stop raise (0030) notify Managers only, with no separate owner send, so they have no overlap to dedupe. 5 regression tests added (`tests/notification-dedupe.test.ts`). | RESOLVED |

Resolved risks will remain listed here (state changed to RESOLVED) rather than deleted.
